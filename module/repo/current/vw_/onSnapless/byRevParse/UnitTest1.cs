using Microsoft.VisualStudio.TestTools.UnitTesting;
using System;
using System.Diagnostics;

namespace nilnul.fs._git_._TEST_.module.repo.current.vw_.onSnapless.byRevParse
{
	[TestClass]
	public class UnitTest1
	{
		[TestMethod]
		public void TestMethod1()
		{
			var detached = module_.unborn.UnitTest1.CreateTmpAsAddress();
			nilnul.fs.folder.explore_._ByExeSelfX.OfAddress(detached);
			try
			{
				var r = nilnul.fs.git.module.repo.current_.reified.snap._Vw_byRevParseX.Txt(detached);
				Debug.WriteLine(r);
				throw new Exception();

			}
			catch (Exception)
			{

				//throw;
			}

			//fs.git.module.shelf._CreateX._Branch(detached, "shelf");
		}
	}
}
